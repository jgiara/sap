$('#toggle-vis-vols-0').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-1').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-2').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-3').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-4').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-5').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-6').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-7').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-8').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-9').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-10').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-11').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-12').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-13').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-14').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});
$('#toggle-vis-vols-15').on( 'click', function (e) {
    e.preventDefault();
    var column = tableVols.column($(this).prop('value'));
    column.visible(!column.visible());
});